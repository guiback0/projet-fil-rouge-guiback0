{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'id': 'userForm', 'class': 'needs-validation'}}) }}
    <div class="row g-4">
        <!-- Informations personnelles -->
        <div class="col-12">
            <h6 class="text-primary mb-3">
                <i class="bi bi-person"></i>
                Informations personnelles
            </h6>
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.prenom, {
                'label': 'Prénom *',
                'attr': {
                    'class': 'form-control',
                    'placeholder': 'Jean'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.nom, {
                'label': 'Nom *',
                'attr': {
                    'class': 'form-control',
                    'placeholder': 'Dupont'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.email, {
                'label': 'Email *',
                'attr': {
                    'class': 'form-control',
                    'placeholder': 'jean.dupont@example.com'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.password, {
                'label': 'Mot de passe *',
                'attr': {
                    'class': 'form-control',
                    'placeholder': 'Mot de passe sécurisé'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.date_naissance, {
                'label': 'Date de naissance',
                'attr': {
                    'class': 'form-control'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.telephone, {
                'label': 'Téléphone',
                'attr': {
                    'class': 'form-control',
                    'placeholder': '01 23 45 67 89'
                }
            }) }}
        </div>
        
        <div class="col-12">
            {{ form_row(form.adresse, {
                'label': 'Adresse',
                'attr': {
                    'class': 'form-control',
                    'placeholder': '12 rue de la République, 75001 Paris'
                }
            }) }}
        </div>
        
        <!-- Séparateur -->
        <div class="col-12">
            <hr>
            <h6 class="text-primary mb-3">
                <i class="bi bi-briefcase"></i>
                Informations professionnelles
            </h6>
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.poste, {
                'label': 'Poste',
                'attr': {
                    'class': 'form-control',
                    'placeholder': 'Développeur Senior'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.jours_semaine_travaille, {
                'label': 'Jours de travail/semaine',
                'attr': {
                    'class': 'form-control',
                    'min': '1',
                    'max': '7',
                    'placeholder': '5'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.heure_debut, {
                'label': 'Heure de début',
                'attr': {
                    'class': 'form-control'
                }
            }) }}
        </div>
        
        <div class="col-md-6">
            {{ form_row(form.horraire, {
                'label': 'Durée journalière',
                'attr': {
                    'class': 'form-control'
                }
            }) }}
        </div>
        
        <!-- Séparateur -->
        <div class="col-12">
            <hr>
            <h6 class="text-primary mb-3">
                <i class="bi bi-shield-check"></i>
                Permissions et accès
            </h6>
        </div>
        
        <div class="col-12">
            <div class="mb-3">
                {{ form_label(form.roles, 'Rôles') }}
                <div class="row mt-2">
                    {% for role in form.roles %}
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card role-card h-100">
                                <div class="card-body">
                                    <div class="form-check">
                                        {{ form_widget(role, {'attr': {'class': 'form-check-input'}}) }}
                                        <label class="form-check-label w-100" for="{{ role.vars.id }}">
                                            <div class="d-flex align-items-start">
                                                <div class="role-icon me-3">
                                                    {% if 'ROLE_ADMIN' in role.vars.value %}
                                                        <i class="bi bi-shield-fill-check text-primary fs-5"></i>
                                                    {% elseif 'ROLE_MANAGER' in role.vars.value %}
                                                        <i class="bi bi-person-gear text-primary fs-5"></i>
                                                    {% else %}
                                                        <i class="bi bi-person text-primary fs-5"></i>
                                                    {% endif %}
                                                </div>
                                                <div class="flex-grow-1">
                                                    <div class="fw-medium">{{ role.vars.label }}</div>
                                                    <small class="text-muted">
                                                        {% if 'ROLE_ADMIN' in role.vars.value %}
                                                            Accès complet au système
                                                        {% elseif 'ROLE_MANAGER' in role.vars.value %}
                                                            Gestion des utilisateurs et services
                                                        {% else %}
                                                            Accès standard aux fonctionnalités
                                                        {% endif %}
                                                    </small>
                                                </div>
                                            </div>
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>


        {% if form.secondary_services is defined %}
        <div class="col-12">
            <div class="mb-3">
                {{ form_label(form.secondary_services, 'Services secondaires') }}
                <div class="row mt-2">
                    {% for service in form.secondary_services %}
                        <div class="col-md-6 col-lg-4 mb-3">
                            <div class="card service-card h-100">
                                <div class="card-body">
                                    <div class="form-check">
                                        {{ form_widget(service, {'attr': {'class': 'form-check-input'}}) }}
                                        <label class="form-check-label w-100" for="{{ service.vars.id }}">
                                            <div class="d-flex align-items-start">
                                                <div class="service-icon me-3">
                                                    <i class="bi bi-briefcase text-primary fs-5"></i>
                                                </div>
                                                <div class="flex-grow-1">
                                                    <div class="fw-medium">{{ service.vars.label }}</div>
                                                    <small class="text-muted">Service additionnel</small>
                                                </div>
                                            </div>
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% endif %}

        {% if form.date_inscription is defined %}
        <div class="col-md-6">
            {{ form_row(form.date_inscription, {
                'label': 'Date d\'inscription',
                'attr': {
                    'class': 'form-control'
                }
            }) }}
        </div>
        {% endif %}
        
        {{ form_rest(form) }}
        
        <!-- Information alert for organisation context -->
        {% if organisation is defined %}
        <div class="col-12">
            <div class="alert alert-info mt-3">
                <i class="bi bi-info-circle"></i>
                <strong>Information:</strong> Cet employé sera automatiquement assigné au service principal de l'organisation {{ organisation.nomOrganisation }}. Vous pouvez également l'assigner à des services secondaires.
            </div>
        </div>
        {% endif %}
        
        <div class="col-12">
            <div class="d-flex justify-content-end gap-2 mt-4">
                <a href="{{ organisation is defined ? path('app_organisation_show', {'id': organisation.id}) : path('app_user_index') }}" class="btn btn-secondary">
                    <i class="bi bi-x-circle"></i> Annuler
                </a>
                <button type="submit" class="btn btn-primary" id="submitBtn">
                    <span class="spinner-border spinner-border-sm d-none" role="status" aria-hidden="true"></span>
                    <i class="bi bi-check-circle"></i> {{ button_label|default('Enregistrer') }}
                </button>
            </div>
        </div>
    </div>
{{ form_end(form) }}

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('userForm');
    const submitBtn = document.getElementById('submitBtn');
    const spinner = submitBtn.querySelector('.spinner-border');
    
    // Real-time validation
    const inputs = form.querySelectorAll('input, select, textarea');
    inputs.forEach(input => {
        input.addEventListener('blur', function() {
            validateField(this);
        });
        
        input.addEventListener('input', function() {
            if (this.classList.contains('is-invalid')) {
                validateField(this);
            }
        });
    });
    
    // Form submission
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        let isValid = true;
        inputs.forEach(input => {
            if (!validateField(input)) {
                isValid = false;
            }
        });
        
        if (isValid) {
            // Show loading state
            submitBtn.disabled = true;
            spinner.classList.remove('d-none');
            
            // Submit form
            this.submit();
        } else {
            // Scroll to first error
            const firstError = form.querySelector('.is-invalid');
            if (firstError) {
                firstError.scrollIntoView({ behavior: 'smooth', block: 'center' });
                firstError.focus();
            }
        }
    });
    
    function validateField(field) {
        const value = field.value.trim();
        const fieldName = field.name;
        let isValid = true;
        let errorMessage = '';
        
        // Clear previous validation states
        field.classList.remove('is-valid', 'is-invalid');
        const feedback = field.parentNode.querySelector('.invalid-feedback');
        if (feedback) feedback.remove();
        
        // Required field validation
        if (field.hasAttribute('required') && !value) {
            isValid = false;
            errorMessage = 'Ce champ est obligatoire.';
        }
        
        // Email validation
        else if (fieldName.includes('email') && value) {
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(value)) {
                isValid = false;
                errorMessage = 'Veuillez saisir un email valide.';
            }
        }
        
        // Password validation
        else if (fieldName.includes('password') && value) {
            if (value.length < 6) {
                isValid = false;
                errorMessage = 'Le mot de passe doit contenir au moins 6 caractères.';
            }
        }
        
        // Phone validation
        else if (fieldName.includes('telephone') && value) {
            const phoneRegex = /^(?:(?:\+|00)33|0)\s*[1-9](?:[\s.-]*\d{2}){4}$/;
            if (!phoneRegex.test(value.replace(/\s/g, ''))) {
                isValid = false;
                errorMessage = 'Veuillez saisir un numéro de téléphone français valide.';
            }
        }
        
        // Days of work validation
        else if (fieldName.includes('jours_semaine_travaille') && value) {
            const days = parseInt(value);
            if (isNaN(days) || days < 1 || days > 7) {
                isValid = false;
                errorMessage = 'Le nombre de jours doit être entre 1 et 7.';
            }
        }
        
        // Apply validation result
        if (isValid && value) {
            field.classList.add('is-valid');
        } else if (!isValid) {
            field.classList.add('is-invalid');
            
            // Add error message
            const errorDiv = document.createElement('div');
            errorDiv.className = 'invalid-feedback';
            errorDiv.textContent = errorMessage;
            field.parentNode.appendChild(errorDiv);
        }
        
        return isValid;
    }
});
</script>

<style>
/* Role and Service Card Styling - Matches Access Form */
.role-card, .service-card {
    transition: all 0.2s ease;
    border: 2px solid transparent;
}

.role-card:hover, .service-card:hover {
    border-color: #0d6efd;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

.role-card .form-check-input:checked ~ .form-check-label,
.service-card .form-check-input:checked ~ .form-check-label {
    color: #0d6efd;
    font-weight: 600;
}

.form-check-input:checked ~ .form-check-label .role-card,
.form-check-input:checked ~ .form-check-label .service-card {
    border-color: #0d6efd;
    background-color: rgba(13, 110, 253, 0.05);
}

.role-icon, .service-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: #e9ecef;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.form-check-input:checked ~ .form-check-label .role-icon,
.form-check-input:checked ~ .form-check-label .service-icon {
    background-color: rgba(13, 110, 253, 0.1);
}

.form-check-label {
    cursor: pointer;
}

/* Service Assignment Card Styling */
.service-assignment-card {
    border: 2px solid #28a745;
    border-radius: 12px;
    background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
    color: white;
    margin-bottom: 1rem;
}

.service-card-content {
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.service-icon-wrapper {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
}

.service-info {
    flex: 1;
}

.service-title {
    font-weight: 600;
    font-size: 0.9rem;
    opacity: 0.9;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.service-name {
    font-weight: 700;
    font-size: 1.2rem;
    margin: 0.25rem 0;
}

.service-description {
    font-size: 0.9rem;
    opacity: 0.8;
}

.service-badge .badge {
    font-size: 0.8rem;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.2) !important;
    color: white !important;
}
</style>