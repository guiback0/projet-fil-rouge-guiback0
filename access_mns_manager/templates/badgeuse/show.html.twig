{% extends 'base.html.twig' %}

{% block title %}Badgeuse{% endblock %}

{% block body %}
    <h1>Badgeuse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ badgeuse.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ badgeuse.reference }}</td>
            </tr>
            <tr>
                <th>Date_installation</th>
                <td>{{ badgeuse.dateInstallation ? badgeuse.dateInstallation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_badgeuse_index') }}">back to list</a>

    <a href="{{ path('app_badgeuse_edit', {'id': badgeuse.id}) }}">edit</a>

    {{ include('badgeuse/_delete_form.html.twig') }}
{% endblock %}
