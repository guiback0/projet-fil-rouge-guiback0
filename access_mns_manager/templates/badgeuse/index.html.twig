{% extends 'base.html.twig' %}

{% block title %}Badgeuses index{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">
            <i class="bi bi-tablet"></i>
            Badgeuses ({{ badgeuses|length }})
        </h5>
        <a href="{{ path('app_badgeuse_new') }}" class="btn btn-sm btn-outline-light">
            <i class="bi bi-plus-circle"></i> Nouvelle badgeuse
        </a>
    </div>
    {% if badgeuses|length > 0 %}
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Référence</th>
                        <th>Date installation</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for badgeuse in badgeuses %}
                    <tr class="clickable-row" data-href="{{ path('app_badgeuse_show', {'id': badgeuse.id}) }}">
                        <td>{{ badgeuse.id }}</td>
                        <td>
                            <div class="fw-medium">{{ badgeuse.reference }}</div>
                        </td>
                        <td>{{ badgeuse.dateInstallation ? badgeuse.dateInstallation|date('d/m/Y') : '' }}</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a href="{{ path('app_badgeuse_show', {'id': badgeuse.id}) }}" class="btn btn-sm btn-primary" title="Voir">
                                    <i class="bi bi-eye"></i>
                                </a>
                                <a href="{{ path('app_badgeuse_edit', {'id': badgeuse.id}) }}" class="btn btn-sm btn-outline-secondary" title="Modifier">
                                    <i class="bi bi-pencil"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <div class="text-center py-4">
            <i class="bi bi-tablet text-muted" style="font-size: 3rem;"></i>
            <p class="text-muted mt-2">Aucune badgeuse</p>
            <a href="{{ path('app_badgeuse_new') }}" class="btn btn-primary">
                <i class="bi bi-plus"></i>
                Créer une badgeuse
            </a>
        </div>
    {% endif %}
</div>
{% endblock %}

{% block stylesheets %}
<style>
.clickable-row {
    cursor: pointer;
    transition: background-color 0.2s ease;
}
.clickable-row:hover {
    background-color: rgba(13, 110, 253, 0.1) !important;
}
</style>
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', () => {
    // Make table rows clickable
    document.querySelectorAll('.clickable-row').forEach(row => {
        row.addEventListener('click', (e) => {
            // Don't trigger if clicking on buttons or links
            if (e.target.closest('a, button')) {
                return;
            }
            window.location.href = row.dataset.href;
        });
    });
});
</script>
{% endblock %}
